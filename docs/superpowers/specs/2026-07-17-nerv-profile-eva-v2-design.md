# GitHub 프로필 NERV v2 — 에바 풀 터미널 디자인 스펙

- 날짜: 2026-07-17 (v1 리스킨 배포 당일 사용자 피드백으로 개정)
- 선행 스펙: `2026-07-17-nerv-profile-reskin-design.md` — **§2·§3 전면 대체**, 파이프라인·검증 골격은 유지
- 피드백 원문: "도면집이 별로야. NERV 강한 에반게리온 스타일로 해봐."

## 1. v1 대비 확정 변경

| 항목 | v1 (기각) | v2 (확정) |
|---|---|---|
| 프레이밍 | 도면집 — FIG 연번·SHEET·DOC NO. 헤더·치수선·등록마크·스탬프 | **전면 폐기.** 에바 터미널 어휘로 교체 |
| 팔레트 | NERV 레드 + 아이보리 (whitepaper 기조) | **NERV 오렌지 `#ff6a00` 주 강조** + MAGI 앰버 `#ffb000`, 웜 잉크(`magi-tokens.css` 그대로). 레드 `#ff2d2d`는 경보 전용 |
| 라이트 모드 | NERV 공문서 인쇄본 (먹+레드 2색, 정적) | **폐기. 다크 단일 룩을 양 모드 공통 적용** — `-light.svg`는 다크와 동일 내용 (README 마크업 유지 목적으로 파일만 분리) |
| 일본어 | 히어로 1곳 | **대폭 확대** — 존 헤더 한자 병기, 대형 고스트 한자 백드롭, `警告` 경보 태그 |
| 모션 | 다크만 | 양 모드 동일 (스캔라인·blink·alertpulse, reduced-motion 대응 유지) |

## 2. v2 디자인 토큰 (SSOT: `magi-tokens.css`)

- 보이드 `#050609` / `#0a0c12` / 패널 `#10131c`
- NERV 오렌지 `#ff6a00` · 하이 `#ff9233` · 딤 `#8a3c00`
- MAGI 앰버 `#ffb000` / 경보 레드 `#ff2d2d` / OK 그린 `#39d98a`
- 웜 잉크 `#ffe2c4` (제목) / `#d9a877` (본문) / `#9a7048` (딤)
- 그리드 = 오렌지 저투명 (헥스 패턴 + 미세 격자)

## 3. v2 공통 어휘

- **헥사곤 그리드 백드롭** (A.T. 필드 문법, 오렌지 op ~.06)
- **CRT 스캔라인 텍스처** + 비네트 (콘텐츠 아래 레이어)
- **해저드 스트라이프** (오렌지/블랙 사선 테이프) — 상단 엣지
- **레드 글로우 헤더 룰** — 우측 핫 세그먼트 (whitepaper `nerv-header::after` 문법)
- **티커 스트립** — 하단 밴드, `>` 프롬프트 + `·` 구분 상태 피드, `警告` 세그먼트는 레드
- **`[ P-01 ]` 브래킷 태그** (앰버), **`▣` 존 헤더 + 한자 병기**
- **MAGI 트라이어드** — MELCHIOR·1 / BALTHASAR·2 / CASPER·3 삼각 배치 + 중앙 CONSENSUS 3/3 노드 (히어로 우측, 기존 2원 래티스 대체)
- 대형 고스트 한자 백드롭 (`協働` 등, 오렌지 op ~.07)
- 원작 NERV 로고 도형 금지 (유지). 콘텐츠 영문 카피·링크·R-01 서지 유지 (유지)

## 4. 자산별 요점

- **히어로**: 해저드 탑 엣지 → 헤더존(eyebrow `NERV // TAEHYEONG LIM, PH.D.` + 우측 그린 SYSTEM NOMINAL) → 레드 글로우 룰 → 좌측 HUMAN × AI(× 오렌지)·CO-INTELLIGENCE SYSTEMS·協働知能システム·서브카피 → 우측 MAGI 트라이어드 + 고스트 한자 → 하단 티커(45 AGENTS·MAGI CONSENSUS 3/3·캡슐 3문구·警告 세그먼트)
- **버튼**: 보이드 + 오렌지 글로우 아웃라인 + 좌측 해저드/스테이터스 노치, 웜 잉크 텍스트
- **연구 지도**: `▣ AI SYSTEMS ARCHITECTURE — 研究システム構成` 존 헤더, 패널 `#10131c` + 오렌지 스트로크, HITL만 레드 + `警告` 태그 + alertpulse, 배선 오렌지 실선/레드 점선
- **NERV 카드**: 대형 타이틀 유지(웜 잉크) + 오렌지 글로우 CTA, 방사 7노드를 헥스 노드로, MAGI VALIDATED 헥스 배지
- **필드 리절트**: `▣ FIELD RESULTS — 実績記録` 존 헤더, `[ R-01 ]` 행(좌측 오렌지 액센트 바, `row-r01` 그룹 유지), PUBLISHED 태그 오렌지 글로우
- **액티비티**: PALETTES 양 키 동일 값(NERV 단일 룩), SHEET/DOC 문구 제거, `BUILD ACTIVITY — 稼働記録` 헤더, 앰버 막대 + 레드 피크 유지
- **README**: FIG.05 `<img alt>`의 "Blueprint-style" 1단어만 갱신 (그 외 불변)

## 5. 검증

v1 절차 계승(테스트 스위트·xmllint·헤드리스 크롬·보존 문구 grep·시안 게이트 2단계 + push 게이트). 변경: 라이트 정적 검증 폐기(양 모드 동일), 보존 문구 목록에서 FIG/SHEET/DOC 계열 제거, `-light.svg`·`-dark.svg` 내용 동일성 확인 추가.
